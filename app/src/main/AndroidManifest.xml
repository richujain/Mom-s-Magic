<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pulimoottil.richu.momsmagickeralafoodrecipe">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SplashScreen"
            android:theme="@style/SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".UserLogin" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".CategorizedItems" />
        <activity android:name=".YoutubeFoodRecipeSearch" />
        <activity android:name=".FavouritesActivity" />
        <activity android:name=".Settings" />
        <activity android:name=".GroceryListActivity" />

        <provider
            android:name=".GroceryProvider"
            android:authorities="com.pulimoottil.richu.momsmagickeralafoodrecipe"
            android:exported="false" />

        <activity android:name=".ReportProblem" />
        <activity android:name=".AboutActivity" />
        <activity android:name=".CreditsActivity" />
        <activity android:name=".HealthTips"></activity>
    </application>

</manifest>